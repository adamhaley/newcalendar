var tree = new intervals.SegmentTree;

      // tree.pushInterval(parseInt(timeStart), parseInt(timeEnd));

      _.each(rows,function(row){
        tree.pushInterval(parseInt(row.time_start), parseInt(row.time_end));

      });
      
      tree.buildTree();
      // console.log(tree.queryOverlap(parseInt(timeStart), parseInt(timeEnd)));
      console.log(tree.queryInterval(parseInt(timeStart), parseInt(timeEnd)));
      tree.printTree();
      tree.clearIntervalStack();

      overlappingEvents = rows;
